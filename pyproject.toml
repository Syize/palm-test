[project]
name = "palm-test"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "fprettify",
]

[tool.uv.sources]
fprettify = { git = "https://github.com/Syize/fprettify" }
