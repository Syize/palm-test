[project]
name = "palm-test"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "charset-normalizer>=3.4.5",
    "fprettify",
    "numpy>=2.4.2",
]

[tool.uv.sources]
fprettify = { git = "https://github.com/Syize/fprettify" }
